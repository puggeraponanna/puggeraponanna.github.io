# Repo notes

- Push directly to `master` — repo is a single-contributor personal site, PRs aren't usable due to required approval rules.
- Build/serve: `bundle exec jekyll build` / `bundle exec jekyll serve` (restart required after `_config.yml` changes).
