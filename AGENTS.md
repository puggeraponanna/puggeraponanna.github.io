# Repo notes

- Never push directly to `master` — it is a protected branch. Always create a feature branch and open a PR via `gh pr create`.
- Build/serve: `bundle exec jekyll build` / `bundle exec jekyll serve` (restart required after `_config.yml` changes).
